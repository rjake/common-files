# options ----
options(
  repos = c(CRAN = "https://cran.rstudio.com/"),
  scipen = 999,
  digits = 3 ,
  tibble.print_max = 30,
  warnPartialMatchArgs = TRUE
)

# make FALSE when needing to update devtools, rstudioapi, etc
if (TRUE) {
  source("~/r_functions.R")
}
